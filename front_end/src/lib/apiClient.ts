// Wrapper for API client with common configurations
import { apiClient as baseApiClient } from '../services/api';

// Re-export the existing API client with additional utilities if needed
export const apiClient = baseApiClient;

// Add any additional utility functions here if needed