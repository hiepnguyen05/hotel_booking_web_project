import { useState } from "react";
import { Button } from "../ui/button";

export function CustomAlertDialogExample() {
  return (
    <div className="flex flex-col items-center gap-4 p-4">
      <h2 className="text-2xl font-bold">Ví dụ Custom Alert Dialog</h2>
      <p className="text-gray-600">Component đã được tạo thành công. Bạn có thể sử dụng nó trong các component khác.</p>
    </div>
  );
}
